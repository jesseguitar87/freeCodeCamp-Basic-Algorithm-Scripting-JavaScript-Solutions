function confirmEnding(str, target) {
  let compare = str.substr(str.length - target.length, str.length);
  if (compare === target) {
    return true;
  }
  return false;
}

confirmEnding("Bastian", "n");
